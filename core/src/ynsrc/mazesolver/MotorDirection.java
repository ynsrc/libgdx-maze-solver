package ynsrc.mazesolver;

/**
 * Motor direction for DC motor simulation.
 */
public enum MotorDirection {
    /** None means DC motor is in idle status. */
    NONE,

    /** Forward means DC motor is normally polarized. */
    FORWARD,

    /** Reverse means DC motor is reverse polarized. */
    REVERSE
}
